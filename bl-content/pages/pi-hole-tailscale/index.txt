This might be the perfect [Pi-hole](https://pi-hole.net/) setup!

Now when I am on the go not only can I access my home network with ease I am also able to take advantage of the ad blocking of [Pi-hole](https://pi-hole.net/).

Here are the basic steps to get things setup. There are more configuration steps available for each installation. Please check their websites for full details before proceeding.

**Step 1:**

Install [Pi-hole](https://pi-hole.net/). This can be done on a Raspberry Pi, in a VM, or any server on your local network.

```
curl -sSL https://install.pi-hole.net | bash
```

### Note:
> In the [Pi-hole](https://pi-hole.net/) Admin page in Settings > DNS, make sure that Listen on all interfaces, permit all origins is selected.
> 
> Tailscale traffic comes in on the tailscale0 network interface, so this option is needed to allow your [Pi-hole](https://pi-hole.net/) to respond to Tailscale-based DNS traffic. When using this option, make sure your [Pi-hole](https://pi-hole.net/) is properly firewalled.

**Step 2:**

Install [Tailscale](https://tailscale.com/) on the server where you installed [Pi-hole](https://pi-hole.net/).

```
curl -fsSL https://tailscale.com/install.sh | sh
```

**Step 3:**

Enable IP forwarding on the server where you installed  [Pi-hole](https://pi-hole.net/).

```
echo 'net.ipv4.ip_forward = 1' | sudo tee -a /etc/sysctl.conf
echo 'net.ipv6.conf.all.forwarding = 1' | sudo tee -a /etc/sysctl.conf
sudo sysctl -p /etc/sysctl.conf
```

**Step 4:**

Start [Tailscale](https://tailscale.com/) on the server where you installed [Pi-hole](https://pi-hole.net/).

```
sudo tailscale up --advertise-exit-node --advertise-routes=192.168.1.0/24 --accept-dns=false --accept-routes=true
```

**Step 5:**

Login to [Tailscale](https://tailscale.com/) and click the "three dots" menu next to the Machine entry for the [Pi-hole](https://pi-hole.net/). Select "Disable key expiry" from the drop-down. Since the [Pi-hole](https://pi-hole.net/) is not directly exposed to the internet it is safe to not require re-authentication. Then select "Edit route settings..." from the same drop-down menu and enable both options. See image below.

<div class="centered">
    <img src="/bl-content/uploads/pages/b2824f25debdfd4c320c3717545b05c8/tailscale-setup.png" class="image-border" /><br><br>
</div>

Next copy the [Tailscale](https://tailscale.com/) IP address for [Pi-hole](https://pi-hole.net/) and use it as the Global Nameservers on the DNS tab. Set them override local DNS. See image below.

<div class="centered">
    <img src="/bl-content/uploads/pages/b2824f25debdfd4c320c3717545b05c8/tailscale-dns.png" class="image-border" /><br><br>
</div>

Turn on MagicDNS on the DNS tab. This way the [Pi-hole's](https://pi-hole.net/) interfaces will work when on or off the Tailscale network using their hostnames.

**Step 6:**

Configure your router to use the [Pi-hole's](https://pi-hole.net/) local IP addresses for your DNS.

**Step 7:**

Install [Tailscale](https://tailscale.com/) on other devices such as your phone so you can connect to your [Tailscale](https://tailscale.com/) network.

Thats it!

Now you should have all your devices setup with ad blocking and VPN. Now when you are connected to your [Tailscale](https://tailscale.com/) network you will also get ad blocking even when you are not on your local network.

Don't forget if you want everyone on your home network (even without being on [Tailscale](https://tailscale.com/)) to be covered by [Pi-hole](https://pi-hole.net/) to update your router's DNS configuration. Instructions can be found [here](https://docs.pi-hole.net/main/post-install/).

**Updates:**
> *I have updated to remove Orbital Sync instructions since it is now deprecated.*
> 
> *I have updated the instructions to use Orbital Sync instead of the now deprecated Gravity Sync.*
> 
> *I have moved my Pi-hole install to an LXC running in Proxmox. So now I only have 1 Pi-hole running. I might eventually spin up another LXC container for redundancy.*
>
> *I have had issues with my DNS using the override local DNS option in [Tailscale](https://tailscale.com/). I have disabled that for now to see if that helps.* 
